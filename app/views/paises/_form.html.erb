<%= form_for(@pais) do |f| %>
  <% if @pais.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pais.errors.count, "error") %> prohibited this pais from being saved:</h2>

      <ul>
      <% @pais.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
