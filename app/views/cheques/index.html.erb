<h1><%= t('cheque.index.title') %></h1>

<table>
  <tr>
    <th>Numero</th>
    <th>Valor</th>
    <th>Vencimento</th>
    <th>Agencia</th>
    <th>Conta</th>
    <th>Banco</th>
    <th>Emitente</th>
    <th>Cliente</th>
    <th>Socio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cheques.each do |cheque| %>
  <tr>
    <td><%= cheque.numero %></td>
    <td><%= cheque.valor %></td>
    <td><%= cheque.vencimento %></td>
    <td><%= cheque.agencia %></td>
    <td><%= cheque.conta %></td>
    <td><%= cheque.banco %></td>
    <td><%= cheque.emitente %></td>
    <td><%= cheque.cliente %></td>
    <td><%= cheque.socio %></td>
    <td><%= link_to t('cheque.index.show_link_label'), cheque %></td>
    <td><%= link_to t('cheque.index.edit_link_label'), edit_cheque_path(cheque) %></td>
    <td><%= link_to t('cheque.index.destroy_link_label'), cheque, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<%= will_paginate @cheques %>

<br />

<%= link_to 'New Cheque', new_cheque_path %>
