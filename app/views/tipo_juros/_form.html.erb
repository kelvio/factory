<%= form_for(@tipo_juro) do |f| %>
  <% if @tipo_juro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_juro.errors.count, "error") %> prohibited this tipo_juro from being saved:</h2>

      <ul>
      <% @tipo_juro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
