<%= form_for(@tipo_despesa) do |f| %>
  <% if @tipo_despesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_despesa.errors.count, "error") %> prohibited this tipo_despesa from being saved:</h2>

      <ul>
      <% @tipo_despesa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
