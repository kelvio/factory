<%= form_for(@tipo_operacao_financeira) do |f| %>
  <% if @tipo_operacao_financeira.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_operacao_financeira.errors.count, "error") %> prohibited this tipo_operacao_financeira from being saved:</h2>

      <ul>
      <% @tipo_operacao_financeira.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
