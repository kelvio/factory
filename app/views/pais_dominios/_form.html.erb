<%= form_for(@pais_dominio) do |f| %>
  <% if @pais_dominio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pais_dominio.errors.count, "error") %> prohibited this pais_dominio from being saved:</h2>

      <ul>
      <% @pais_dominio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit t('general.save') %>
  </div>
<% end %>
