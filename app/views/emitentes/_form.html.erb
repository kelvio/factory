<%= form_for(@emitente) do |f| %>
  <% if @emitente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emitente.errors.count, "error") %> prohibited this emitente from being saved:</h2>

      <ul>
      <% @emitente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
